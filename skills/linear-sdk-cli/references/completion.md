# linear completion

> Output a shell completion script (bash|zsh|fish)

_Generated from `linear commands --json`. `linear completion --help` (or `<subcommand> --help`) is authoritative._

Every command also accepts the global flags `-j/--json`, `--no-input`, `-y/--yes`, `-q/--quiet`, `--workspace <slug>`, `--api-key <key>`, `-t/--team <key>`, `-n/--limit <n>`, `--all`, `-f/--fields <a,b,c>`, `--no-color`, and `--debug`. Only command-specific options are listed below.

### `linear completion`

Output a shell completion script (bash|zsh|fish)

```
linear completion [options] <shell>
```
